<div class="container">
  <div class="row">
    <h1 class="m-3 border-bottom border-danger"><%= @venue.name %></h1>


    <div class="justify-content-center col-8 m-3">
      <div id="carouselExampleControls" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
            <% @venue.photos.each_with_index do |photo, index| %>
              <% if index == 0 %>
                <div class="carousel-item active">
              <% else %>
                <div class="carousel-item">
              <% end %>
                <img src="<%= cl_image_path photo.key %>" class="d-block w-100 rounded" alt="...">
              </div>
            <% end %>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Previous</span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="visually-hidden">Next</span>
        </button>
      </div>
      </div>
      <br>
        <div class="card w-50 m-3">
          <div class="card-body">
            <br>
            <h5 class="card-title border-bottom border-danger p-2"><%= @venue.description %></h5>
            <br>
            <h5 class="card-text border-bottom border-danger p-2">📍<%= @venue.location %></h5>
            <br>
            <h5 class="card-text border-bottom border-danger p-2">💃<%= @venue.capacity %> Person Capacity</h5>
            <br>
            <h5 class="card-text p-2">💰 £<%= @venue.price_per_hour %> per hour</h5>
          </div>
        </div>
    </div>
  </div>
</div>
