<div class="container">
  <br>
    <h1>Your Bookings</h1>
  <br>
    <div class="row">
      <% @bookings.each do |booking| %>
        <div class="card col-6 m-3 p-2" style="width: 18rem;">
          <img src=<%= cl_image_path booking.venues.photos[0].key %> class="card-img-top" alt="booking-photo">
          <div class="card-body">
            <h5 class="card-title border-bottom border-danger">💃 <%= booking.venues.name %></h5>
            <h5 class="card-title border-bottom border-danger">💰 £<%= booking.total_price %></h5>
            <h5 class="card-title">📅 <%= "#{booking.end_date - booking.start_date}" %></h5>
          </div>
      <% end %>
    </div>
</div>
